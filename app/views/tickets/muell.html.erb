<div class="container-fluid">
  
	<div class="row">
    <div class="col-md-12">
      <div class="panel-heading header">
        <h1>
		    <% if user_signed_in? %>
		      <% if current_user.id == @sponsor.event.company.user.id %>
		        <%= link_to new_ticket_path :sonsor_id => @sponsor.id do %>
		          <i class="btn btn-primary btn-lg glyphicon glyphicon-plus"></i>
		        <% end %>
		    <% end %>
        Tickets (<%= @sponsor.event.name + " " + @sponsor.company.name  %>)
        <span class="badge"><%= @ticanz %></span>
        </h1>
      </div>
    </div>
  </div>
  <br>
  
  <% ticketcount = 0 %>
  <% @tickets.each do |t| %>
    <div class="col-sm-6 col-md-4 col-lg-3">
      <div class="thumbnail" align="center">
  		</div>
		</div>
  <% end %>

</div>